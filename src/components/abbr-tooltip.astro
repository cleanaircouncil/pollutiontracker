---
const { title } = Astro.props;
---

<abbr class="tooltip" title={title} data-tooltip><slot /></abbr>
<style>
  .tooltip {
    position: relative;
    cursor: help;
  }

  .tooltip:hover {
    text-decoration-style: solid;
  }

  .tooltip::before {
    content: attr(title);
    display: block;
    position: absolute;
    background: var(--color-white, "#FFF");
    color: var(--color-black);
    font-size: inherit;
    padding: 0.5rem;
    border-radius: 0.25rem;
    box-shadow: var(--box-shadow);
    width: max-content;
    max-width: 150px;
    text-align: center;
    left: 50%;
    top: 50%;
    font-weight: bold;

    opacity: 0;
    translate: -50% -100%;

    transition:
      opacity 0.2s ease-in-out,
      translate 0.2s ease-in-out;

    transition-delay: 0s;
  }

  .tooltip:hover::before {
    opacity: 1;
    translate: -50% -120%;
    transition-delay: 0.5s;
  }
</style>
